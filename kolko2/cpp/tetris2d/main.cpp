#include <iostream>

using namespace std;

const int MAXN = 1e6 + 7;
int wysokosci[MAXN], szerokosc_platformy, szerokosc_klocka, poczatek_klocka,
    il_klockow, i;

int main() {
    cin >> szerokosc_platformy >> il_klockow;
    for (i = 0; i < szerokosc_platformy; ++i) {
        cin >> szerokosc_klocka >> poczatek_klocka;
    }
}