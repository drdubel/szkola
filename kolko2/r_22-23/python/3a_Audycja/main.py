def slowoszukacz(tekst, dl_tekstu):
    for litera, odw_litera in zip(tekst, reversed(tekst)):
        if


def main():
    dl_tekstu = int(input())
    tekst = input()
    print(slowoszukacz(tekst, dl_tekstu))
